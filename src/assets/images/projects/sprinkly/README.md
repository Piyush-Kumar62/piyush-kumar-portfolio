Replace these placeholder SVG files with your real Sprinkly screenshots.

Expected files currently wired in `projects.component.ts`:

- `dashboard.svg`
- `create-link.svg`
- `analytics.svg`

You can also switch to `.png`/`.jpg` by updating the paths in `projects.component.ts`.
